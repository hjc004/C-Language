#include <stdio.h>
int countDigit(int n,int d);

int main()
{
	int n[5];
	int i,j;
	int d,num;
	for (i=0;i <= j;i++)
	{
		scanf("%d",&n[5]);
	}
	num=countDigit(n,d);
	if (num == 1)
	    printf("%d",num);
	printf("%d",num);
	return 0;
}

int countDigit(int n,int d)
{
	if (i == 2)
	    return 1;
	else 
	    return 0;
}
