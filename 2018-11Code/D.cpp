#include <stdio.h>

typedef struct book
{
	char name[31];
	float price;
}A;

int main()
{
	A a[10];
	int n,i;
	scanf("%d",n);
	for (i = 0;i < n;i++)
	{
		scanf("%s%f",message[i].name,&message[i].price);
	}
	return 0;
}
